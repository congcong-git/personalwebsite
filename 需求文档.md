# 个人网站需求文档（v0.7 · 补齐到可上线）

> 状态：**✅ P0–P3 代码全部交付（lint / 类型检查 / 两种构建 / 运行验证 均通过）**。
> 当前后台走 **A 路线：腾讯云云开发「免费体验版」+ 文档型数据库 + `@cloudbase/node-sdk`，¥0**；**云开发 CMS 扩展（Web 真后台）留作升级路径，需付费个人版**。A股 widget 已确认不纳入；前端部署走 EdgeOne Pages（免费、国内快）。
>
> 本轮补齐（P2/P3）：RSS、sitemap/robots、OG 图、JSON-LD、站内搜索、Giscus 评论、Umami 统计、公众号展示、PDF 简历、后台会话鉴权、EdgeOne 部署配置、GitHub Actions CI、README 重写。
>
> 部署要点：EdgeOne Pages 要求 Next.js **静态导出**，故分两种构建——`npm run build`（常规/CI）与 `npm run build:export`（部署，产出 `out/`）。静态导出下无服务端接口，管理端仅本地可用。详见 §11 验收标准与 README。
>
> 整理人：Buddy　更新：2026-09-18
> 工作目录：`E:\code\personnal-website`

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 定位 | 个人品牌 / 自媒体主页（配合公众号沉淀技术形象；**不含 A股 / 行情内容**） |
| 目标用户 | 同行工程师、潜在合作方、读者（**国内为主**） |
| 语言 | **中英双语（zh + en）** |
| 核心目标 | 展示个人技术形象、沉淀双语博客、对接公众号与社交账号 |
| 四大板块 | 关于我/简历、项目展示、博客/文章、友链/联系方式 |
| 内容管理 | **当前：云开发免费体验版「文档型数据库」（node-sdk 构建时拉取）；升级：云开发 CMS 扩展（个人版）** |

---

## 2. 技术栈（已确认）

- **框架**：Next.js 16（App Router）+ TypeScript
- **样式**：Tailwind CSS v4
- **国际化**：`[locale]` 路由 + `next-intl`（zh 为主，en 镜像结构）
- **内容**：**云开发文档型数据库**（构建时 `@cloudbase/node-sdk` 读取集合生成静态页）；未配环境变量时回退本地 `content/` 目录（开发/验证用）
- **渲染**：纯静态 SSG，构建时拉取 CloudBase 数据，便于 EdgeOne Makers 免费托管
- **依赖**：`next` `react` `typescript` `tailwindcss` `next-intl` `gray-matter` `rehype-highlight` `lucide-react` `next-themes` `@cloudbase/node-sdk`(构建时拉数据) `giscus` `pagefind`/`flexsearch`(搜索) `umami`/`plausible`(统计)
- **不选** Decap CMS / Astro / Nuxt / 纯静态手编 / 付费 CMS / 自建服务器 / Supabase / Lighthouse 的理由：
  - 用户要**独立账号 + 角色权限 + 富媒体库**的真后台 → 最终由云开发 CMS 满足；免费期先用文档库过渡
  - Supabase 国内慢 + 7 天无流量暂停；Lighthouse 需付费运维
  - CloudBase 存内容 + EdgeOne Makers 部署，两个产品均免费/低价、国内快、零运维

---

## 3. 站点结构与板块内容

### 3.1 信息架构（双语）

```
/[locale]/                首页（Hero + 入口卡片 + 最新文章/精选项目 + 社交导航）
/[locale]/about           关于我 / 简历
/[locale]/projects        项目展示（卡片列表）
/[locale]/projects/[slug] 项目详情
/[locale]/blog            博客列表（分页 + 标签筛选 + 语言筛选）
/[locale]/blog/[slug]     文章详情（Markdown + 代码高亮）
/[locale]/links           友链 / 联系方式
/admin（外部，升级后）    云开发 CMS 后台（独立地址，CloudBase 账号登录）
```

### 3.2 各板块内容细节

- **首页**：Hero → 入口卡片（博客/项目/关于）→ 最新文章 & 精选项目各 3 条 → 社交图标。数据来自 CloudBase 集合（或本地回退）。
- **关于我 / 简历**：个人简介、技能栈标签、经历时间线、PDF 简历下载。
- **项目展示**：`projects` 集合，字段含名称、简介、技术栈、角色、封面、链接、亮点、正文。
- **博客 / 文章**：`posts` 集合，字段含标题、日期、标签、摘要、语言、封面、正文（Markdown）。列表分页 + 标签/语言筛选；详情 Markdown + 代码高亮 + 阅读时长 + 上/下篇。
- **友链 / 联系**：`links` 集合 + GitHub / 邮箱 / 公众号入口。

---

## 4. 功能需求清单（已确认）

| 类别 | 功能 | 优先级 | 状态 |
|---|---|---|---|
| 多语言 | 中英双语 + 语言切换 | P0 | ✅ |
| 基础 | 响应式布局 | P0 | ✅ |
| 基础 | 浅色为主 + 暗色切换 | P0 | ✅ |
| 基础 | SEO 元数据 + sitemap + OG 图 | P0 | ✅ |
| 博客 | Markdown 渲染 + 代码高亮 + 标签筛选 + 分页 | P1 | ✅（筛选/分页改为客户端，因静态导出不支持 searchParams） |
| 博客 | RSS 订阅（`/feed.xml`） | P1 | ✅ |
| 博客 | 站内搜索 | P2 | ✅ 零依赖中文分词 + 加权排序 |
| 博客 | 评论（Giscus） | P2 | ✅ 代码就绪，配置 `NEXT_PUBLIC_GISCUS_*` 后生效 |
| 项目 | 卡片列表 + 详情页 | P1 | ✅ |
| 简历 | PDF 下载 | P1 | ✅ 占位已生成，待替换为真实简历 |
| 分析 | 访问统计（Umami） | P2 | ✅ 代码就绪，配置 `NEXT_PUBLIC_UMAMI_*` 后生效 |
| 后台 | **云开发文档库（免费体验版，node-sdk）+ CMS 升级路径** | P1 | ✅ 免费路径 |
| 后台 | 管理端鉴权（会话 Cookie + sha256） | P3 | ✅ 替换了明文口令头 |
| 动态 | 公众号展示（关于页二维码板块） | P2 | ✅ 占位二维码待替换 |
| 动态 | 文章「来源」标注 | P2 | ✅ `source` 字段（公众号名或原文链接） |
| 部署 | EdgeOne Pages 静态导出 + CI | P3 | ✅ `edgeone.json` + `.github/workflows/ci.yml` |
| 动态 | ~~A股 / 行情 widget~~ | — | ❌ **已确认不纳入** |

---

## 5. 后台管理端（Admin / 数据层）

### 5.1 当前方案（A 路线 · 免费体验版文档库）

| 项 | 说明 |
|---|---|
| 环境 | **云开发「免费体验版」**（2026 起需一次性兑换码，3000 资源点/月，可续期 6 个月，¥0） |
| 数据 | **文档型数据库**（MongoDB 兼容）+ 云存储（富媒体）+ 云函数 |
| 读取 | 构建时 `@cloudbase/node-sdk` 的 `database()` 读集合，SSG 生成静态页 |
| 写入（后台） | 免费体验版**无 CMS Web UI** → 过渡期用：① 云开发控制台手动加数据；② `tcb` CLI；③ 可选 dev-only 最小 admin 页（本地 `pnpm dev` 访问） |
| 成本 | **¥0**（文档库/存储/函数共享 3000 点池，个人站用量极低） |
| 真后台体验 | ⚠️ 免费期偏弱（无独立 Web UI）；**升个人版装 CMS 后自动补全** |
| 升级路径 | 站成型且愿付费 → 升个人版 → 装 CMS 扩展 → Web 后台 + 角色权限 + 富媒体管理，**代码零改**（`cloudbase.ts` 已写好） |

### 5.2 升级方案（B 路线 · 云开发 CMS 扩展，需付费个人版）

| 项 | 说明 |
|---|---|
| 形态 | 云开发 CMS 扩展（独立后台地址，非站点 `/admin` 子路径） |
| 登录 | CloudBase 账号体系，支持管理员/运营者账号 + **角色权限** |
| 富媒体 | 云存储，后台上传图片/附件，文章内嵌图走 CloudBase CDN |
| 写入流程 | CMS 编辑 → 写文档库 → Webhook/云函数 → 触发 GitHub Actions 重建 → EdgeOne 部署 |
| 成本 | **个人版 ¥19.9/月（限时优惠）**，资源点基本在免费额度内 ≈¥0（有基础套餐费，非纯零） |
| 关键约束 | **CMS 扩展需按量/付费环境**；免费体验版不支持按量付费 → **装不了 CMS** |

> ⚠️ 纠正（v0.5 错误假设）：免费体验版**能开文档库、能 node-sdk 读取**，但**装不了 CMS 扩展**。所以"FREE 装 CMS"不成立；CMS 必须升个人版。

---

## 6. 设计风格

- 基调：简洁、清爽、略带运动感（透明背景 + 灰边框 + 无重阴影）
- 配色：浅色为主，主色冷调（靛蓝/青），强调色用于链接与 CTA
- 字体：中文系统字体栈；代码等宽
- 动效：克制，仅 hover/入场轻微过渡

---

## 7. 部署方案（EdgeOne Makers + CloudBase，均免费/低价）

| 方案 | 费用 | 国内速度 | 结论 |
|---|---|---|---|
| **EdgeOne Makers（免费）** | 免费额度（流量/函数/存储/构建） | 快 | **采用**：前端 SSG 部署 |
| **CloudBase 免费体验版** | 3000 点/月，¥0 | 快 | **采用（当前）**：文档库 + 构建时拉数据 |
| CloudBase 个人版 + CMS | ¥19.9/月 | 快 | 升级路径：真后台 Web UI |
| Vercel | 免费 | ❌ 偶尔抽 | 弃用（国内慢） |
| Supabase | $0 | ❌ 慢+暂停 | 弃用 |

**部署架构**：
1. **内容层**：云开发文档库（免费体验版）管理内容；CMS 上线后由 CMS 写入同一文档库。
2. **构建层**：Next.js 构建时 `@cloudbase/node-sdk` 读集合 → SSG。
3. **部署层**：静态产物部署 EdgeOne Makers。
4. **更新闭环**：CMS 改内容 → Webhook → GitHub Actions 重建 → EdgeOne 部署（升级后生效；免费期手动重建）。

---

## 8. 内容数据来源

| 内容 | 来源 | 集合 |
|---|---|---|
| 博客文章 | 文档库（CMS 升级后后台编辑） | `posts` |
| 项目信息 | 文档库 | `projects` |
| 关于/简历 | 文档库 | `profile`（单条） |
| 友链 | 文档库 | `links` |
| 公众号文章 | 升级后 CMS 创建/同步 | `posts`（标记来源=公众号） |

---

## 9. 开发里程碑

- **P0**：✅ 脚手架、全局布局、明暗主题、SEO、`[locale]` 路由、首页、关于页（构建+运行双验证通过）
- **P1**：✅ 博客系统（列表+详情+筛选+分页+代码高亮）、项目展示（列表+详情）、内容适配器（本地回退 + CloudBase 适配器）、**免费体验版文档库路线已确认（A 路线）**；本地 `content/` 回退已生效
- **P2**：友链完善、RSS、站内搜索、Giscus 评论、访问统计、sitemap、公众号文章展示
- **P3**：EdgeOne Makers 部署上线 + GitHub Actions CI（含 CMS Webhook 触发重建）
- ~~P4（可选）：A股/行情 widget~~ —— **已确认移除**

---

## 10. 待确认事项（已全部确认）

| # | 问题 | 状态 |
|---|---|---|
| 1 | 语言 | ✅ 中英双语 |
| 2 | 腾讯云部署 | ✅ EdgeOne Makers（前端，免费）+ CloudBase（数据层，免费体验版） |
| 3 | 评论系统 | ✅ Giscus（P2） |
| 4 | RSS/搜索/统计 | ✅ 都要（P2） |
| 5 | 自有域名 | ✅ 暂无，先用平台免费子域 |
| 6 | 动态内容范围 | ✅ 仅公众号展示（P2）；A股 widget 不纳入 |
| 7 | 简历 PDF | ✅ 要（P1 占位） |
| 8 | 后台管理端 | ✅ **当前：免费体验版文档库（¥0）；升级：个人版 CMS（真后台 Web UI）** |

---

## 11. 验收标准

> 已实测通过项打 ✅，未打勾的为「需你提供外部凭证 / 替换占位资源后生效」或「尚未实操」。

- [x] zh/en 双语言可切换、内容可维护
- [x] 四大板块均可访问（首页/博客/项目/关于/友链）
- [x] 博客 Markdown 渲染、代码高亮、标签筛选、分页正常
- [x] **构建时从云开发文档库拉取内容生成静态页（免费体验版验证通过）**
- [x] PDF 简历可下载（占位文件，需替换真实简历）
- [x] RSS（`/feed.xml`）、站内搜索 生效
- [x] SEO：metadata / OG 图 / JSON-LD / `sitemap.xml` / `robots.txt` 就绪
- [x] 明暗主题切换正常
- [x] 管理端会话鉴权（登录 / 会话校验 / 退出 / 伪造会话拒绝）
- [x] 部署构建 `npm run build:export` 产出 `out/`（30 个静态页）
- [x] CI 闸门：lint + 类型检查 + 构建 均通过
- [ ] Giscus 评论生效（★ 需你提供 `NEXT_PUBLIC_GISCUS_*`）
- [ ] Umami 统计生效（★ 需你提供 `NEXT_PUBLIC_UMAMI_*`）
- [ ] 公众号二维码替换为真实二维码
- [ ] EdgeOne Pages 实际部署、国内可访问（需你在控制台导入仓库）

---

## 12. P1 交付 + A 路线（免费体验版文档库）开通步骤

### 12.1 当前 P1 已交付（代码层，已构建+运行验证通过）

- 博客系统：`/[locale]/blog` 列表（标签筛选 + 分页）+ `/[locale]/blog/[slug]` 详情（Markdown + **highlight.js 代码高亮** + 阅读时长 + 上/下篇）+ 双语。
- 项目展示：`/[locale]/projects` 列表 + `/[locale]/projects/[slug]` 详情。
- 内容数据层（`src/lib/content.ts`）：统一异步 API；`src/lib/cloudbase.ts` 为云开发适配器（env 驱动，动态 import `@cloudbase/node-sdk`）。
- **本地回退**：未配置 CloudBase 环境变量时读本地 `content/` 目录（`content/posts/*.md` + 结构化项目/关于/友链），现在即可本地运行/构建，**不依赖云**。
- 已 seed 3 篇中文 + 1 篇英文博客到 `content/posts/`。

### 12.2 A 路线开通步骤（免费体验版文档库，¥0）

代码已支持「检测到 `CLOUDBASE_*` 环境变量即自动从文档库拉数据」。你只需补环境侧：

1. **开免费体验版环境**：腾讯云 → 云开发 → 新建环境 → **免费体验版**（领兑换码，3000 点/月）。⚠️ 地域选**上海**（新加坡仅 PostgreSQL，无文档型库）；**数据库类型选「云数据库（默认）= 文档型」**，切勿选 PostgreSQL。
2. **建 4 个集合**：`posts` / `projects` / `profile` / `links`，字段见 §12.4。
3. **配 API 密钥**：腾讯云「访问管理 → API 密钥管理」新建（主账号密钥，仅需读库权限）；或环境「环境配置 → 环境密钥」。
4. **装 SDK 依赖**（启用拉数必需，本次因执行工具异常未代跑，请手动执行）：
   ```bash
   pnpm add @cloudbase/node-sdk
   ```
5. **配环境变量**：`cp .env.example .env.local`，填 `CLOUDBASE_ENV_ID` / `CLOUDBASE_SECRET_ID` / `CLOUDBASE_SECRET_KEY`（注意三个变量均带 `CLOUDBASE_` 前缀）。
6. **连通验证**（装包后）：
   ```bash
   node --env-file=.env.local scripts/verify-cloudbase.mjs
   ```
   成功会打印 `✅ node-sdk 连通成功（免费版文档库可用）` 并列出 posts 前几条。
7. **重建**：`pnpm build` 自动从文档库拉内容生成静态页。本地 `content/` 回退可保留作离线兜底。

> **✅ A 路线实测通过（2026-09-17）**：免费体验版环境 + 文档型库 + `@cloudbase/node-sdk 3.18.3` 完整跑通。步骤 4 的 `pnpm add` 已代跑完成；`scripts/seed-cloudbase.mjs` 已把本地内容灌入 4 集合（posts 4 / projects 3 / profile 1 / links 2）；`verify-cloudbase.mjs` 连通成功；`pnpm build` 从云端生成全部博客/项目详情页（对照验证：临时移走本地 `content/` 后 build 仍成功，证明构建完全走云端、不再依赖本地回退）。⚠️ `@cloudbase/node-sdk` 是 CommonJS 模块，`import { init }` 命名导入会报 "Named export 'init' not found"，三处（verify/cloudbase.ts/cloudbase-admin.ts）已统一改用 default import 兼容写法。

> 注：`package.json` 的 `name` 已从占位 `scaffold` 改为 `personnal-website`。

### 12.3 免费期后台管理（admin 页 + 会话鉴权，已做）

免费体验版无 CMS Web UI，已补一个**本地调试后台**（默认仅 development 可用）：
- **访问**：`pnpm dev` 后打开 `/zh/admin`（或 `/en/admin`）
- **口令**：默认 `admin`（可设 `ADMIN_PASSWORD` 覆盖，见 `.env.example`）
- **能力**：选集合（posts/projects/profile/links）→ 列出记录 → 字段表单编辑 → 保存/删除
- **字段**：按 §12.4 建模清单填写；新建不带 `_id`，编辑保留 `_id`
- **备选**：若不想用 admin 页，仍可走云开发控制台、`tcb` CLI 或 `scripts/seed-cloudbase.mjs`

#### 鉴权机制（T18 已升级为真会话，不再用明文口令头）

| 环节 | 做法 |
|---|---|
| 登录 | 前端只在 `/api/admin/login` 提交**一次**明文口令 |
| 校验 | 服务端 `sha256` 摘要 + `timingSafeEqual` 定时安全比对，不落明文、不比对明文 |
| 会话 | 下发 `admin_session` Cookie：`HttpOnly` + `SameSite=Lax` + 生产环境 `Secure`，有效期 2 小时 |
| 会话串 | `<过期时间戳>.<随机 nonce>.<HMAC-SHA256>`，服务端无状态即可验签、防伪造 |
| 签名密钥 | 由 `ADMIN_PASSWORD` + `ADMIN_SESSION_SECRET` 派生 → **改口令或改盐，存量会话立即失效** |
| 节流 | 同 IP 10 分钟内错 5 次即拒（10 分钟窗口），挡脚本爆破 |
| 接口守卫 | `/api/admin/*` 一律校验会话 Cookie，无 Cookie/过期/伪造 → 401 |

**环境闸门**：默认仅 `NODE_ENV=development` 可用（生产返回 404，管理端不暴露）。
确需在生产开放时，设 `ADMIN_ALLOW_PRODUCTION=true` —— **务必先改成强口令**，因为线上任何知道口令的人都能读写你的内容库。

> ⚠️ 已知取舍：会话是无状态 HMAC，退出登录只清除浏览器 Cookie，**服务端不会吊销已签发的串**（最多 2 小时后自然过期）。需要立即吊销时，改 `ADMIN_SESSION_SECRET` 并重启，即可让全部存量会话失效。

> 代码位置：`src/app/[locale]/admin/page.tsx`（前端）+ `src/app/api/admin/{login,logout,session}/route.ts` + `src/app/api/admin/[collection]/route.ts` + `src/app/api/admin/upload/route.ts` + `src/lib/admin-auth.ts`（鉴权核心）+ `src/lib/cloudbase-admin.ts`（node-sdk CRUD 封装）。

### 12.4 四集合字段建模清单（对应 `content.ts` 类型 + `cloudbase.ts` 映射）

**posts（博客，多条）**
| 字段 | 类型 | 说明 |
|---|---|---|
| slug | string（唯一） | URL 用，如 `three-gripper-greedy` |
| title | string | 标题 |
| date | string | `YYYY-MM-DD` |
| tags | array<string> | 标签，如 `["算法","机器人"]` |
| excerpt | string | 摘要 |
| lang | string | `zh` 或 `en` |
| cover | string（可选） | 云存储 URL |
| body | string | Markdown 正文 |

**projects（项目，多条）**
| 字段 | 类型 | 说明 |
|---|---|---|
| slug | string（唯一） | 如 `palletizing` |
| name | string | 项目名 |
| summary | string | 简介 |
| tech | array<string> | 技术栈 |
| role | string | 角色 |
| link | string（可选） | 外链 |
| highlight | string | 亮点 |
| cover | string（可选） | 封面 URL |
| body | string（可选） | Markdown 详情 |

**profile（关于，单条）**
| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 如 `xuniw` |
| resumeUrl | string（可选） | 简历 URL，默认 `/resume.pdf` |
| skills | array<string> | 技能栈 |
| timeline | array<{year,title,desc}> | 经历时间线 |

**links（友链，多条）**
| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 名称 |
| url | string | 链接 |
| desc | string | 描述 |

### 12.5 已知小项

- PDF 简历：关于页 `/resume.pdf` 仍为占位，点击 404；后续接 `profile.resumeUrl` 或放 `public/resume.pdf`。
- 代码高亮：`rehype-pretty-code` + `shiki` 在 Next 16 / shiki 4 下同步/异步不兼容，已改用 **`rehype-highlight`（highlight.js）**，明暗双主题已配。
- 升级 CMS 时：`cloudbase.ts` 的 `loadCMSContent()` 已按 `posts/projects/profile/links` 集合结构映射，字段与 §12.4 一致，零代码改动即可切换。
