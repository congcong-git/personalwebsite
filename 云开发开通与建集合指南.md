# 云开发开通与建集合操作清单（A 路线 · 免费体验版文档库）

> 配套：需求文档.md §12。本文件是「开环境 → 建集合 → 填数据 → 接通验证」的一步一步清单。
> **字段名已与 `src/lib/cloudbase.ts` 的读取逻辑逐一核对**，可直接照填，不会因字段名对不上而读不到。
> 适用：CloudBase **免费体验版（¥0）**。装 CMS 扩展需升个人版，本清单不含。

---

## 0. 前置概念（1 分钟）

- 云开发文档库 = MongoDB 兼容的**文档型数据库**。
- "集合"(collection) ≈ 关系型数据库的"表"，但**建集合时不用定义字段（列）**，字段在每条 JSON 记录里自带。
- 我们要建 5 个集合：`posts` / `projects` / `profile` / `links` / `settings`。
- `profile` 只放 **1 条**记录（代码 `limit(1)` 只读第一条，多条忽略多余）。其余 3 个是多条。

---

## 1. 开通免费体验版环境

1. 腾讯云 → 云开发 CloudBase → 新建环境
2. 选 **免费体验版**（领一次性兑换码，3000 资源点/月，可续期 6 个月，¥0）
3. ⚠️ **地域选「上海」**（新加坡地域只有 PostgreSQL，无文档型库，直接排除）
4. ⚠️ **数据库类型选「云数据库（默认）= 文档型」**，切勿选 PostgreSQL（选了就没有文档库，CMS / node-sdk 全废）
5. MySQL 关系型库不用开（文档库够用）
6. 创建后，控制台「环境」页复制 **环境 ID**（形如 `xxx-env-id`），第 5 步要用

---

## 2. 建 4 个集合（控制台）

```
云开发控制台 → 选环境 → 数据库 → 集合列表 → 新建集合
```

逐个新建，名称分别填（英文、小写、和下面**完全一致**）：

- `posts`
- `projects`
- `profile`
- `links`
- `settings`

> 建集合时**只填名字，不用建字段 / 索引**。字段在每条记录里自己带。

---

## 3. 字段建模 + 可直接粘贴的示例 JSON

下面每个集合给「字段表」+「一条完整示例 JSON」。复制 JSON 到控制台「添加记录」或 `/zh/admin` 页面即可。

### 3.1 posts（博客，多条）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| slug | string | ✅ | URL 标识，唯一，如 `three-gripper-greedy` |
| title | string | ✅ | 标题 |
| date | string | ✅ | `YYYY-MM-DD` |
| tags | array\<string\> | ✅ | 标签，如 `["算法","机器人"]` |
| excerpt | string | ✅ | 列表页摘要 |
| lang | string | ✅ | `zh` 或 `en`（其它值一律当 `zh`） |
| cover | string | ❌ | 云存储图片 URL，留空则无封面 |
| source | string | ❌ | 首发来源：公众号名（如 `xuniw 的技术笔记`）或原文 URL；填了文章页显示「来源」徽章 |
| body | string | ✅ | Markdown 正文（全文） |

示例（一篇中文博客）：

```json
{
  "slug": "three-gripper-greedy",
  "title": "三抓手码垛的贪心算法实践",
  "date": "2026-08-12",
  "tags": ["算法", "机器人"],
  "excerpt": "五花垛如何用贪心策略分层放置，并处理跨层竖包补充。",
  "lang": "zh",
  "cover": "",
  "source": "xuniw 的技术笔记",
  "body": "# 三抓手码垛的贪心算法实践\n\n核心难点在于横包可合并、竖包需独立旋转放置……（此处粘贴完整 Markdown 正文）"
}
```

> `source` 填 URL（`http(s)://` 开头）→ 徽章变为可点击的「查看原文」；填普通文本 → 显示「来源：xxx」。留空则不显示。

> 英文博客：再建一条，`lang` 填 `en`，`slug` 取不同值（如 `three-gripper-greedy-en`）。`/en/blog` 只列 `lang=en` 的文章。

### 3.2 projects（项目，多条）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| slug | string | ✅ | 唯一，如 `palletizing` |
| name | string | ✅ | 项目名 |
| summary | string | ✅ | 简介 |
| tech | array\<string\> | ✅ | 技术栈 |
| role | string | ✅ | 你的角色 |
| link | string | ❌ | 外链 URL |
| highlight | string | ✅ | 亮点一句话 |
| cover | string | ❌ | 封面 URL |
| body | string | ❌ | Markdown 详情（可空） |

示例：

```json
{
  "slug": "palletizing",
  "name": "三抓手码垛程序",
  "summary": "自动化码垛核心逻辑，贪心算法解决五花垛分层放置。",
  "tech": ["算法", "机器人", "C#"],
  "role": "核心开发",
  "link": "",
  "highlight": "每层 5 包（2 横 + 3 竖），按层级从低到高放置。",
  "cover": "",
  "body": "## 项目背景\n\n三抓手码垛需要在有限空间内稳定堆叠五花垛……（完整 Markdown 见 content/ 或自行编写）"
}
```

### 3.3 profile（关于我，单条 ⚠️）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | ✅ | 如 `xuniw` |
| resumeUrl | string | ❌ | 简历 URL，留空默认 `/resume.pdf` |
| wechat | object | ❌ | 公众号展示（见下）。留空则「关于」页不显示公众号板块 |
| skills | array\<string\> | ✅ | 技能栈标签 |
| timeline | array\<{year,title,desc}\> | ✅ | 经历时间线 |

`wechat` 子字段：

| 子字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | ❌ | 公众号名称 |
| qr | string | ❌ | 二维码图片路径 / URL（**有值才渲染整个板块**） |
| desc | string | ❌ | 一句话介绍，留空用内置文案 |

⚠️ **此集合只放 1 条**；代码 `limit(1)` 只读第一条，多条会忽略多余的。

示例（单条）：

```json
{
  "name": "xuniw",
  "resumeUrl": "/resume.pdf",
  "wechat": {
    "name": "xuniw 的技术笔记",
    "qr": "/wechat-official-qr.svg",
    "desc": ""
  },
  "skills": ["自动化码垛", "机器人编程", "算法设计", "Vue / Nuxt", "Next.js", "Node.js / Express", "MySQL", "Ansible", "Java 持久层", "A股短线框架"],
  "timeline": [
    { "year": "2024", "title": "三抓手码垛项目", "desc": "主导五花垛贪心算法与跨层补充逻辑调试。" },
    { "year": "2025", "title": "约球吧小程序", "desc": "从 0 到 1 完成 33 页小程序与管理后台。" },
    { "year": "2026", "title": "个人技术站", "desc": "Next.js + 云开发文档库 + EdgeOne Makers 全栈部署。" }
  ]
}
```

> 二维码占位图 `public/wechat-official-qr.svg` 部署前请替换为真实公众号二维码（可上传到云存储后用其 URL，或直接替换该文件）。

### 3.4 links（友链，多条）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | ✅ | 站点 / 友人称谓 |
| url | string | ✅ | 链接 |
| desc | string | ✅ | 描述 |

示例（两条）：

```json
{ "name": "陈小群的短线笔记", "url": "https://example.com", "desc": "A股情绪周期与龙头战法整理" }
{ "name": "某技术博客", "url": "https://example.com", "desc": "前端与全栈实践" }
```

### 3.5 settings（站点设置，单条 ⚠️）

全站「站点名 / 品牌字标 / 关于页简介 / 页脚简介 / 社交链接 / SEO 文案」都放这里，由后台 `/zh/admin` 的「站点设置」维护，不再写死在代码里。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| siteName | string | ✅ | 站点名称（标题模板、OG、JSON-LD 用） |
| brand | string | ❌ | 顶栏 / 页脚品牌字标（默认 `xuniw`） |
| bio | string | ❌ | 关于页顶部简介 |
| footerNote | string | ❌ | 页脚简介 |
| socials | array\<{type,url,label}\> | ❌ | 社交链接；`type` 取 `github`/`email`/`twitter`/`wechat`/`link`（决定图标），`url` 为链接，`label` 可选显示名 |
| seo | object | ❌ | 按语言：`seo.zh{title,description}`、`seo.en{title,description}` |

⚠️ **此集合只放 1 条**；代码 `limit(1)` 只读第一条。

示例（单条）：

```json
{
  "siteName": "xuniw 的技术站",
  "brand": "xuniw",
  "bio": "自动化码垛 / 机器人编程工程师。这里记录技术实践、项目复盘与一些思考。",
  "footerNote": "自动化码垛与机器人编程工程师的个人技术站，记录工程实践与思考。",
  "socials": [
    { "type": "github", "url": "https://github.com/xuniw", "label": "GitHub" },
    { "type": "email", "url": "mailto:hello@xuniw.dev", "label": "Email" }
  ],
  "seo": {
    "zh": { "title": "xuniw 的技术站", "description": "自动化码垛 / 机器人编程工程师的个人技术站：博客、项目与思考。" },
    "en": { "title": "xuniw's Tech Blog", "description": "Personal tech blog of an automation & robotics engineer." }
  }
}
```

---

## 4. 填数据的三种方式

1. **云开发控制台**：环境 → 数据库 → 选集合 → 添加记录 → 粘贴上面的 JSON → 保存。（技术人够用）
2. **admin 页（最省事，默认 dev-only）**：
   - 本地 `pnpm dev` 启动
   - 打开 `http://localhost:3000/zh/admin`（或 `/en/admin`）
   - 口令默认 `admin`（可在 `.env.local` 设 `ADMIN_PASSWORD` 覆盖）
   - 登录后凭 `HttpOnly` 会话 Cookie 访问（2 小时有效），刷新页面不会掉登录
   - 选集合 → 列出记录 → 字段表单编辑（含 Markdown 编辑器 + 图片上传）→ 保存 / 删除
   - ⚠️ 仅 `NODE_ENV=development` 可用，生产 API 返回 404，不暴露；需在生产开放请设 `ADMIN_ALLOW_PRODUCTION=true`（先改强口令）
3. **tcb CLI**：命令行 CRUD（适合批量 / 脚本）
4. **一键播种脚本（推荐，最省事）**：把本地 `content/` 与内置示例数据灌入 5 个集合（含 `settings`），**幂等**（按 slug/name/siteName upsert，可反复执行）：
   ```bash
   node --env-file=.env.local scripts/seed-cloudbase.mjs
   ```
   > 新增字段（如 `source` / `wechat`）后，重新执行该脚本即可把本地最新字段同步到线上集合。

---

## 5. 接通验证（填完数据后）

```bash
# 1. 装 SDK（免费版读文档库必需）
pnpm add @cloudbase/node-sdk

# 2. 配密钥（复制模板，填真实值）
cp .env.example .env.local
#   CLOUDBASE_ENV_ID=你的环境ID
#   CLOUDBASE_SECRET_ID=你的密钥ID
#   CLOUDBASE_SECRET_KEY=你的密钥Key
#   ADMIN_PASSWORD=admin   # 可选

# 3. 连通验证（打印 ✅ 即免费版文档库可用）
node --env-file=.env.local scripts/verify-cloudbase.mjs

# 4. 重建（自动从文档库拉内容生成静态页）
pnpm build
```

> 腾讯云「访问管理 → API 密钥管理」新建密钥（主账号密钥，仅需读库权限）；或用环境「环境配置 → 环境密钥」。

---

## 6. 常见坑

- ❌ 地域选了新加坡 → 只有 PostgreSQL，无文档型库 → 重开上海环境
- ❌ 数据库类型选 PostgreSQL → 没有文档库，node-sdk 读不到 → 重开选「云数据库（默认）」
- ❌ `profile` 放了多条 → 只认第一条，多余忽略 → 保持单条
- ❌ `settings` 放了多条 → 只认第一条，多余忽略 → 保持单条
- ❌ `lang` 填成 `zh-CN` 之类 → 代码只认 `zh` / `en`，其它当 `zh`
- ❌ env 变量名写错前缀 → 必须是 `CLOUDBASE_ENV_ID` / `CLOUDBASE_SECRET_ID` / `CLOUDBASE_SECRET_KEY`（全带 `CLOUDBASE_` 前缀）
- ⚠️ 免费体验版装不了 CMS 扩展（需按量 / 付费环境）；升个人版后同一套集合原样接管，代码零改
